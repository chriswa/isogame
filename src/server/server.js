import staticServer from './staticServer.js'
import userWebsocketServer from './userWebsocketServer.js'
import version from './version.js'

import chalk from 'chalk'
console.log()
console.log(chalk.white.bgBlue.bold(' === SERVER START === ') + ' ' + chalk.yellow(new Date()))
